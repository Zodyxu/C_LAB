// Control Structure
// C program to check positive number
//simple-if

#include<stdio.h>
int mian()
{
    int x;
    printf("enter the number:");
    scanf("%d", &x);

    if(x>0)
    {
        printf("positive number %d\n", x);
    }
        return 0;
}