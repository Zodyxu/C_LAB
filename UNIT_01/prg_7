//C program for swapping two numbwes
#include<stdio.h>
int main()
{
    int a, b, temp;
    printf("Enter the swap value of a and b:");
    scanf("%d %d", &a, &b);
    
    temp=a;
    a=b;
    b=temp;
    printf("After swapping:\n a: %d\n b: %d\n", a, b);
    return 0;
}